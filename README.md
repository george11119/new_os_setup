## Setup script:

A script to run which will automatically set up a newly installed debian based linux distro to my liking.

## To use:

1. copy this folder and all its contents into the newly installed distro
2. `cd` into the folder, `chmod +x` the script, then run the script with `sudo` privileges
